<%= form_for(@anyshit) do |f| %>
  <% if @anyshit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anyshit.errors.count, "error") %> prohibited this anyshit from being saved:</h2>

      <ul>
      <% @anyshit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
